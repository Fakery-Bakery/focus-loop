import { TimerControls, RandomPauseConfig } from './types';
/**
 * Creates a pause timer with randomized duration within the specified range
 */
export declare function createPauseTimer(config: RandomPauseConfig): TimerControls;
